<Page
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Test"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:uaCrl="using:UmiAoi.UWP.Controls"
    xmlns:uaBeh="using:UmiAoi.UWP.Behaviors"
    xmlns:Interactivity="using:Microsoft.Xaml.Interactivity" 
    xmlns:Core="using:Microsoft.Xaml.Interactions.Core"
    x:Class="Test.MainPage" 
    mc:Ignorable="d">
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel>
            <uaCrl:GifView UriSource="ms-appx:///Images/sample.gif" Stretch="None"/>
            <ListBox>
                <ListBoxItem>
                    <Button Width="100" Content="bbb">
                        <Interactivity:Interaction.Behaviors>
                            <uaBeh:FocusScaleBehavior/>
                        </Interactivity:Interaction.Behaviors>
                    </Button>
                </ListBoxItem>
                <ListBoxItem HorizontalContentAlignment="Center">
                    <TextBlock>
                        <Run Text="bbb"/>
                    </TextBlock>
                </ListBoxItem>
            </ListBox>
            <Border BorderThickness="1" BorderBrush="Aqua">
                <Canvas Height="100">
                    <Button Content="b">
                        <Interactivity:Interaction.Behaviors>
                            <uaBeh:DragBehavior/>
                        </Interactivity:Interaction.Behaviors>
                    </Button>
                </Canvas>
            </Border>
        </StackPanel>
    </Grid>
</Page>
